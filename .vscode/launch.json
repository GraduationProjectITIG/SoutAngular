{
    "configurations": [
        {
            "type": "chrome",
            "name": "http://this.post.audio",
            "request": "launch",
            "url": "http://localhost:4200/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}